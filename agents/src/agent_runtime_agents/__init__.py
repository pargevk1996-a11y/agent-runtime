"""Reference agents.

Executor, critic, verifier, and planner implementations built strictly on the
public runtime API. The critic-executor-verifier pattern lives here as a
supported composition, deliberately *not* baked into the engine core.
"""

__version__ = "0.1.0"
