[project]
name = "agent-runtime-api"
version = "0.1.0"
description = "FastAPI control plane for agent-runtime (create/subscribe/cancel/replay runs)."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agent_runtime_api"]
